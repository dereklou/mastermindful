'use strict';
import React, {
  StyleSheet,
} from 'react-native';

const TimerInputCSS = StyleSheet.create({
  timerView: {
    flex: 1
  },

  timerInput: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1
  }

});

module.exports = TimerInputCSS;
