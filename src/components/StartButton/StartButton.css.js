'use strict';
import React, {
  StyleSheet,
} from 'react-native';

const StartButtonCSS = StyleSheet.create({
 startButtonView: {
	flex: .1,
	borderColor: 'gray',
	borderWidth: 1
  },

  startButtonText: {
    textAlign: 'center',
  }

});

module.exports = StartButtonCSS;
